---
name: Build disk images

on:
  workflow_dispatch:
    inputs:
      upload-to-s3:
        description: "Upload to S3"
        required: false
        default: false
        type: boolean
      platform:
        required: true
        type: choice
        options:
          - amd64
          - arm64
  workflow_run:
    # Chain off the stable image build instead of guessing with a cron: the
    # MODS run (Friday 07:00 UTC, or any push to mods/**) dispatches the image
    # build, and this fires once that run completes on main. The job-level
    # `if` below skips failed/cancelled image builds, so the ISO is only ever
    # rebuilt from a freshly pushed bluefin-dx:stable.
    workflows: ["Build dx4homelab/bluefin/dx Stable"]
    types: [completed]
    branches: [main]
  pull_request:
    branches:
      - main
    paths:
      - './disk_config/disk.toml'
      - './disk_config/iso.toml'
      - './.github/workflows/build-disk.yml'

env:
  REPO_NAME: ${{ github.event.repository.name }} # output of build.yml, keep in sync
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"  # do not edit
  DEFAULT_TAG: "latest"
  BIB_IMAGE: "quay.io/centos-bootc/bootc-image-builder:latest" # "quay.io/centos-bootc/bootc-image-builder:latest" - see https://github.com/osbuild/bootc-image-builder/pull/954
  S3_BUCKET: "dx4homelab"
  S3_PREFIX: "iso"
  AWS_REGION: "us-east-2"
  ISO_ISSUE_TITLE: "ISO downloads (weekly build)"
  ISO_ISSUE_LABEL: "iso-download"

jobs:
  build:
    name: Build disk images
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    # Job-level (not workflow-level) on purpose: a workflow_run fired by a
    # failed/cancelled image build is skipped by the `if` above and must not
    # enter the group, otherwise it would cancel an ISO build in progress.
    # Matrix values are part of the key so matrix legs never cancel each other.
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref || github.run_id }}-${{ matrix.image-flavor }}-${{ matrix.disk-type }}
      cancel-in-progress: true
    # Default to amd64 unless arm64 is explicitly chosen (workflow_run/PR have no inputs).
    runs-on: ${{ inputs.platform == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-24.04' }}
    strategy:
      fail-fast: false
      matrix:
        disk-type: ["anaconda-iso"]
        image-flavor: ["bluefin-dx:stable"]
        # image-flavor: ["bluefin-dx-nvidia-open:stable","bluefin-dx:stable"]
    permissions:
      contents: read
      packages: read
      id-token: write
      issues: write # rolling "ISO downloads" issue, see "Publish download link" step

    steps:
      - name: Prepare environment
        run: |
          USER_UID=$(id -u)
          USER_GID=$(id -g)
          # Concatenate the types with a hyphen
          DISK_TYPE=$(echo "${{ matrix.disk-type }}" | tr ' ' '-')
          IMAGE_FLAVOR="${{ matrix.image-flavor }}"
          # Lowercase the image uri
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_FLAVOR=${IMAGE_FLAVOR,,}" >> ${GITHUB_ENV}
          echo "DISK_TYPE=${DISK_TYPE}" >> ${GITHUB_ENV}
          echo "USER_UID=${USER_UID}" >> ${GITHUB_ENV}
          echo "USER_GID=${USER_GID}" >> ${GITHUB_ENV}

      - name: Install dependencies
        if: inputs.platform == 'arm64'
        run: |
          set -x
          sudo apt update -y
          sudo apt install -y \
            podman

      - name: Maximize build space
        if: inputs.platform != 'arm64'
        uses: ublue-os/remove-unwanted-software@cc0becac701cf642c8f0a6613bbdaf5dc36b259e # v9
        with:
          remove-codeql: true

      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6

      - name: Build disk images
        id: build
        uses: osbuild/bootc-image-builder-action@main
        with:
          builder-image: ${{ env.BIB_IMAGE }}
          config-file: ${{ matrix.disk-type == 'anaconda-iso' && './disk_config/iso.toml' || './disk_config/disk.toml' }}
          image: ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_FLAVOR }}
          chown: ${{ env.USER_UID }}:${{ env.USER_GID }}
          types: ${{ matrix.disk-type }}
          additional-args: --use-librepo=True --rootfs=ext4

      - name: Upload disk images and Checksum to Job Artifacts
        if: github.event_name == 'workflow_dispatch' && inputs.upload-to-s3 != true
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          path: ${{ steps.build.outputs.output-directory }}
          if-no-files-found: error
          retention-days: 0
          compression-level: 0
          overwrite: true
      
      - name: Generate ISO checksum
        # Written into the build output dir so the recursive S3 upload below
        # ships it next to the ISO as <iso>.sha256 (sha256sum -c compatible).
        if: github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && inputs.upload-to-s3 == true)
        id: checksum
        run: |
          set -euo pipefail
          out="${{ steps.build.outputs.output-directory }}"
          iso=$(find "$out" -type f -name '*.iso' | head -n1)
          if [ -z "$iso" ]; then
            echo "::error::no .iso found under $out"; exit 1
          fi
          ( cd "$(dirname "$iso")" && sha256sum "$(basename "$iso")" > "$(basename "$iso").sha256" )
          digest=$(skopeo inspect --format '{{.Digest}}' "docker://${IMAGE_REGISTRY}/${IMAGE_FLAVOR}" 2>/dev/null || echo "unknown")
          {
            echo "iso-key=${iso#"$out"/}"
            echo "sha256=$(cut -d' ' -f1 "${iso}.sha256")"
            echo "size=$(numfmt --to=iec-i --suffix=B "$(stat -c %s "$iso")")"
            echo "image-digest=${digest}"
          } >> "$GITHUB_OUTPUT"

      - name: Configure AWS Credentials
        if: github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && inputs.upload-to-s3 == true)
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          role-to-assume: arn:aws:iam::199715917706:role/github-s3-iso-upload-role # The ARN of the role created in Step 3
          aws-region: ${{ env.AWS_REGION }}

      - name: Upload ISO images to S3
        if: github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && inputs.upload-to-s3 == true)
        run: |
          aws s3 cp ${{ steps.build.outputs.output-directory }} s3://${{ env.S3_BUCKET }}/${{ env.S3_PREFIX }}/ --recursive

      - name: Publish download link to GitHub issue
        # One rolling issue (found by label) keeps the latest link in its body;
        # each build adds a comment. The @mention + assignee make GitHub email
        # the repo owner on every build without any SMTP/SES setup.
        if: github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && inputs.upload-to-s3 == true)
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          OWNER: ${{ github.repository_owner }}
          ISO_URL: https://${{ env.S3_BUCKET }}.s3.${{ env.AWS_REGION }}.amazonaws.com/${{ env.S3_PREFIX }}/${{ steps.checksum.outputs.iso-key }}
          ISO_SHA256: ${{ steps.checksum.outputs.sha256 }}
          ISO_SIZE: ${{ steps.checksum.outputs.size }}
          IMAGE_DIGEST: ${{ steps.checksum.outputs.image-digest }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          IMAGE_BUILD_URL: ${{ github.event.workflow_run.html_url || '' }}
        run: |
          set -euo pipefail
          built=$(date -u +'%Y-%m-%d %H:%M UTC')
          iso_file=$(basename "$ISO_URL")
          if [ -n "$IMAGE_BUILD_URL" ]; then
            trigger="chained off [image build](${IMAGE_BUILD_URL})"
          else
            trigger="manual dispatch"
          fi

          details=$(cat <<EOF
          | | |
          |---|---|
          | **ISO** | [${iso_file}](${ISO_URL}) (${ISO_SIZE}) |
          | **SHA-256** | \`${ISO_SHA256}\` ([.sha256](${ISO_URL}.sha256)) |
          | **Image** | \`${IMAGE_REGISTRY}/${IMAGE_FLAVOR}\` |
          | **Image digest** | \`${IMAGE_DIGEST}\` |
          | **Built** | ${built} ([run](${RUN_URL})) |
          | **Trigger** | ${trigger} |

          Verify after download:
          \`\`\`bash
          curl -LO "${ISO_URL}" && curl -LO "${ISO_URL}.sha256" && sha256sum -c "${iso_file}.sha256"
          \`\`\`
          EOF
          )

          gh label create "$ISO_ISSUE_LABEL" --color 0E8A16 --force \
            --description "Rolling issue with the latest ISO download link (updated by build-disk.yml)"

          num=$(gh issue list --label "$ISO_ISSUE_LABEL" --state open --limit 1 --json number --jq '.[0].number // empty')
          if [ -z "$num" ]; then
            url=$(gh issue create --title "$ISO_ISSUE_TITLE" --label "$ISO_ISSUE_LABEL" --assignee "$OWNER" \
              --body "Placeholder, updated by the first build.")
            num="${url##*/}"
            echo "created issue #$num"
          fi

          printf '%s\n' "@${OWNER} new ISO is available." "" "$details" | gh issue comment "$num" --body-file -
          printf '%s\n' "Latest ISO. Each build posts a comment below; this body always reflects the newest one." "" "$details" \
            | gh issue edit "$num" --body-file -

          printf '%s\n' "## ISO download" "" "$details" "" "Posted to issue #${num}." >> "$GITHUB_STEP_SUMMARY"
